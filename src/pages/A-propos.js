import React from 'react';
import Header from '../components/Header';

const Apropos = () => {
    return (
        <div>
            <Header/>
            a propos
        </div>
    );
};

export default Apropos;